# VD01. Введение в веб-разработку. Основы HTML

## Что мы умеем:

✅ Умеем собирать, очищать, обрабатывать данные с помощью библиотеки Pandas

✅ Научились строить различные типы графиков

## Результат дня

Будем понимать, что такое HTML и как он используется в веб-разработке, а также научимся создавать простой HTML-документ и использовать основные HTML-теги.

▶️ Время чтения и просмотра: ~ 1 - 1,5 часа

## Теория на сегодня:

### 1. Этапы анализа данных и сферы его применения

**Сегодня на уроке:**

- рассмотрим, что такое HTML и как он используется в веб-разработке;
- научимся создавать простой HTML-документ;
- ознакомимся с основными HTML-тегами и их использованием.

**Разберем понятия Frontend и Backend.**

💡 **Frontend** — это внешняя часть сайта, которую пользователь наблюдает при посещении (кнопки, ссылки, формы обратной связи, онлайн-чат, текст, фото, видео, аудио материалы).

💡 **Backend** — это внутренняя часть сайта, законы, основа функционирования нашего сайта, включающая в себя логические процесс работы (авторизация пользователей, их регистрация и вход в систему, а также взаимодействие с базами данных и другие аспекты). Это ключевая составляющая, определяющая функционал и работу сайта в целом.

**Рассмотрим, что такое HTML и как он используется в веб-разработке.**

💡 **HTML (Hyper Text Markup Language)** — это стандартный язык разметки для создания веб-страниц. Он используется для структурирования содержимого на веб-странице. HTML позволяет задавать заголовки, абзацы, ссылки, изображения и другие элементы, которые составляют веб-страницу.

Чтобы начать работать с HTML в Python необходимо создать отдельный файл. Это можно сделать двумя способами:

1. `webPy→New→HTML File` (при этом внутри файла автоматически будет создан заготовленный шаблон)
2. `webPy→New→File` (в этом случае необходимо указать название файла полностью `index.html`, а для создания шаблона необходимо прописать `!` и нажать на `Tab`)

**Теги** - это ключевые слова, которые используются для описания и структурирования содержимого веб-страницы (все то, что записано в треугольных скобках). Они являются строительными материалами сайта, аналогичны кирпичикам, из которых состоит его конструкция.

- **Одинарные теги** - это теги, которые не имеют пары. Они записываются самостоятельно внутри треугольных скобок.
- **Парные теги** - это теги, которые имеют начало и конец. В конце закрывающего парного тега всегда присутствует символ "/".

**Разберем базовую структуру HTML.**

HTML очень похож на человека: у него есть тег `<head>`, аналогичный голове, и тег `<body>`, аналогичный телу.

- **Тег `<doctype html>`** (означает "объявление типа документа") сообщает браузеру, какая именно версия HTML используется на странице. Этот тег должен всегда находиться в первой строке каждой страницы.

- **Тег `<html>`** является корневым элементом, который заключает в себе все содержимое веб-страницы.

- **Тег `<head>`** — это раздел документа, который содержит различные методы и кодировки. Например, кодировка UTF-8 позволяет использовать русский язык и другие символы. Внутри этого тега может содержаться заголовок, ключевые слова, описание страницы и другие метаданные.

- **Тег `<body>`** — раздел документа, который содержит весь контент, отображаемый на сайте. Для добавления изображения, заголовка, текста, ссылки или другого контента на веб-страницу необходимо разместить их между открывающим и закрывающим тегами `<body>`.

- **Тег `<meta>`** — предназначен для предоставления структурированных метаданных о сайте.

- **Тег `<title>`** определяет заголовок документа, который отображается в заголовке окна браузера или на вкладке страницы.

**Пропишем название страницы и проверим ее отображение в браузере.**

Пропишем название страницы: «Первый сайт».

```html
<!doctype html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Первый сайт</title>
</head>
<body>
</body>
</html>
```

2. Для того, что посмотреть, как будет выглядеть название необходимо выбрать в правом верхнем углу браузер через, который будет запущена программа.

3. Проверим отображение страницы в браузере.

### 2. Виды HTML тегов

**Рассмотрим некоторые виды HTML тегов.**

- **Блочные теги** предназначены для структурирования основных частей страницы, путем разделения содержимого на логически связанные блоки. При их использовании вначале и конце автоматически ставится перенос строки, также они занимают всю ширину сайта. Пример: `<h1> </h1>` — заголовок первого уровня.

- **Строчные теги** предназначены, чтобы разграничить часть текста и придать ему определённую функцию или смысл. При их использовании строка не переносится. Пример: `<span></span>`

**Основные теги для работы с текстом на странице.**

- `<h1></h1>` — `<h6></h6>` - блочный тег, предназначен для использования заголовков в тексте, который видит пользователь при переходе на страницу сайта (существует шесть уровней заголовков);
- `<p></p>` — блочный тег, который используется для разделения блоков текста на параграфы или абзацы;
- `<span></span>` — строчный тег, который используется для стилизации части текста (с его помощью можно поменять цвет, размер шрифта или начертание, добавить и убрать подчёркивание).

**Пропишем основные теги и посмотрим их отображение в браузере.**

```html
<h1>1 уровень</h1>
<h2>2 уровень</h2>
<h3>3 уровень</h3>
<h4>4 уровень</h4>
<h5>5 уровень</h5>
<h6>6 уровень</h6>
<p>Это обычный текст параграфа</p>
<p>Это обычный текст параграфа</p>
<span>Текст строчный</span>
<span>Текст строчный</span>
<span>Текст строчный</span>
<span>Текст строчный</span>
```

Посмотрим как прописанные теги отобразятся в браузере.

- **Парные** — имеют открывающий и закрывающий тег, перед именем закрывающего тега ставят знак слеш `/`. Пример: `<h1> </h1>`
- **Одиночные** — те, что не имеют закрывающего тега. Пример: `<hr>`

- `<hr>` — одиночный тег, задающий разделительную горизонтальную линию, которая адаптируется под размер экрана.

**Пропишем тег `<hr>` и посмотрим его отображение в браузере.**

```html
<p>Это текст</p>
<hr>
<p>Это текст</p>
```

Для хорошей читаемости кода и для того чтобы показать вложенность одного тега внутри другого необходимо пользоваться табуляцией (специальным символом, полученным с помощью клавиши Tab).

- `<br>` — это тег переноса строки, он используется для создания новой строки или начала нового абзаца в блоке текста.

**Пропишем тег `<br>` и посмотрим его отображение в браузере.**

```html
<p>У нас открылся магазин с <br> продукцией азиатских стран</p>
```

**Основные теги для работы с ссылками на странице.**

Существуют две вариации ссылок: относительная и абсолютная адресация. Относительная адресация применяется при перемещении внутри одного сайта, например, при переходе к определенному блоку или между страницами на этом же сайте. Абсолютная адресация используется при открытии внешнего сайта, не связанного с первоначальным.

💡 **Атрибуты** — это дополнительные значения, которые настраивают теги или регулируют их поведение различным способом (позволяют раскрыть полный функционал тегов).

- **Тег `<a>`** нужен для размещения гиперссылок, которые связывают страницу с другой страницей или с определенным местом на той же веб-странице.

- **Атрибут `href`** задаёт адрес документа, на который следует перейти.

Для того чтобы добавить ссылку используем тег `<a>` и атрибут `href`.

```html
<a href="https://zerocoder.ru">этот текст является ссылкой</a>
```

Для того чтобы добавить ссылку и открыть ее в отдельной вкладке используем тег `<a>` и атрибут `target`, который указывает место (окно браузера) в которое должен быть загружен, указанный в гиперссылке документ. Также прописываем `_blank`, что позволяет открывать документ в новом окне.

```html
<a href="https://zerocoder.ru" target="_blank">этот текст является ссылкой</a>
```

При наличии атрибута `download` браузер не переходит по ссылке, а предложит скачать документ, указанный в адресе ссылки. Для этого в атрибуте `href` прописываем название файла для скачивания.

```html
<a href="название файла для скачивания" download>этот текст является ссылкой</a>
```

Для того чтобы использовать относительную адресацию и переместиться внутри одного сайта на другую страницу необходимо в атрибуте `href` прописать название страницы.

```html
<a href="название страницы" target="_blank">этот текст является ссылкой</a>
```

Для того чтобы использовать относительную адресацию и переместиться внутри одного сайта на определенной части одной и той же страницы используются ссылки-якоря. Ссылка-якорь — это обычная ссылка, в адресе которой используется символ `#`, после которого следует идентификатор элемента.

```html
<a href="#test" target="_blank">этот текст является ссылкой</a>
```

- **Атрибут `id`** устанавливает элементу уникальный номер (идентификатор), имя которого должно быть уникальным в документе.

- **Атрибут `class`** - атрибут тега, с помощью которого можно задать имя любому элементу на странице.

### 3. Основные теги для работы с встроенным контентом на странице

1. **`<img>`** — одинарный тег, предназначен для отображения на веб-странице изображений. Он используется с обязательным атрибутом `src`, который указывает путь к изображению, которое необходимо встроить в документ.

Чтобы указать путь к изображению нужно указать название папки, где находиться изображение и название самой картинки.

```html
<img src="название папки/название изображения">
```

Существуют дополнительные необязательные атрибуты, которые могут быть указаны для достижения различных целей.

- Атрибуты `width` и `height` используются когда необходимо задать пространство занимаемое изображением (меняют ширину и высоту изображения). Не рекомендуется использовать сразу два этих атрибута, так как при указании одного, второй автоматически выстраивается пропорционально первому.

```html
<img src="название папки/название изображения" width="300px">
```

- Атрибут `alt` содержит текстовое описание изображения, которое читают пользователи если изображение не может быть загружено по какой-либо причине.

```html
<img src="название папки/название изображения" width="300px" alt="котик">
```

- Атрибут `title` используется для отображения дополнительной информации об изображении при наведении курсора на него (всплывающая подсказка). Этот атрибут может быть полезным для предоставления разъяснений или дополнительных сведений о содержании изображения.

```html
<img src="название папки/название изображения" title="Это изображение котика">
```

2. **`<video></video>`** — парный тег для встраивания видео контента в документ.

- Атрибут `src` задаёт путь к файлу, который нужно воспроизвести.
- Для встраивания видео можно указать ширину, используя атрибут `width`.
- Атрибут `controls` добавляет на страницу элементы управления плеером.
- Атрибут `loop` устанавливает автоматическое повторение аудио после завершения его воспроизведения (делает видео зацикленным).

```html
<video src="название папки/название видео" width="300px" controls loop></video>
```

3. **`<audio></audio>`** — парный тег, который используется для встраивания звукового контента в документ.

Для встраивания аудио файла необходимо использовать атрибут `src`, указать путь (папку и название файла), прописать атрибут `controls`.

Можно добавить атрибут `muted`, который указывает, будет ли звук изначально отключен.

```html
<audio src="название папки/название аудио" controls muted></audio>
```

**Основные теги для работы со списками на странице.**

Списки — это способ группировки текста на странице. Они помогают визуально отделить однородный контент от остального текста: покупки, задачи, пункты инструкции, документы на визу и другое.

- **Маркированный список** подходит для случаев, когда информацию надо просто перечислить и нам не нужна строгая иерархия. (когда добавляются отступы и маркеры слева от списка, чтобы данные не сливались с остальным текстом).

Маркированный список в HTML можно создать с помощью парного тега `<ul></ul>`. В него помещаются элементы списка `<li></li>`.

- **Нумерованный список** можно расположить элементы в строгом порядке.

Нумерованный список создаётся с помощью парного тега `<ol></ol>`. После этого его можно заполнить элементами списка `<li></li>`.

**Пропишем маркированный и нумерованный списки и посмотрим их отображение в браузере.**

```html
<ul>
	<li>первый пункт списка</li>
	<li>второй пункт списка</li>
	<li>третий пункт списка</li>
</ul>

<ol>
	<li>первый пункт списка</li>
	<li>второй пункт списка</li>
	<li>третий пункт списка</li>
</ol>
```

Списки могут состоять из нескольких вложенных подпунктов.

```html
<ol>
	<li>Техника</li>
	<li>
		Продукты
		<ul>
			<li>Хлеб</li>
			<li>Молоко</li>
			<li>Чипсы</li>
		</ul>
	</li>
	<li>Одежда</li>
</ol>
```

**Основные теги для работы с таблицами на странице.**

- **Тег `<table></table>`** используется для представление таблиц в HTML.
- Для создания строки таблицы прописывается тег `<tr></tr>` (это один горизонтальный ряд).
- **Тег `<td></td>`** определяет ячейку таблицы которая содержит данные. В ячейку можно прописывать и изображение, и видео, и аудио контент.
- Если необходимо прописать заголовок применяется тег `<th></th>`.

Для придания стиля полученной таблице можно воспользоваться атрибутами:

- атрибут `bgcolor` указывает цвет фона ячейки заголовка таблицы;
- атрибут `align` задает горизонтальное выравнивание содержимого абзаца;
- атрибут `border` позволяет установить рамки (толщину) вокруг элемента;
- атрибут `height` устанавливает высоту таблицы;
- атрибут `width` устанавливает ширину таблицы.

**Создадим таблицу.**

```html
<table bgcolor="pink" align="centre" border="2px" height="400px" width="800px">
	<tr>
		<th>Название фильма</th>
		<th>Жанр</th>
		<th>Оценка на кинопоиске</th>
	</tr>
	<tr>
		<td>Вонка</td>
		<td>Мюзикл</td>
		<td>7.2/10</td>
	</tr>
	<tr>
		<td>Дюна</td>
		<td>Фантастика</td>
		<td>7.2/10</td>
	</tr>
	<tr>
		<td>Интерстеллар</td>
		<td>Научная фантастика</td>
		<td>8.8/10</td>
	</tr>
</table>
```

Посмотрим отображение созданной таблицы в браузере.

- **Тег `<div></div>`** — это контейнер для разделов сайта, который группирует блочные элементы. Для стилизации данного тега используется атрибут `class`.

**Пропишем базовую структуру сайта, где:**

- `menu` — это верхняя часть сайта, где находиться непосредственно меню;
- `main` — это основная часть сайта с контентом;
- `footer` — это нижняя часть сайта (подвал), где размещается контактная информация.

```html
<div class="menu">
	<a></a>
</div>
<div class="main">
	<h1></h1>
	<p></p>
</div>
<div class="footer">
	<p></p>
</div>
```

## 4. Результаты урока

**Результаты урока:**

- поняли, что такое HTML и как он используется в веб-разработке;
- научились создавать простой HTML-документ;
- познакомились с основными HTML-тегами и их использованием.

## Дополнительные материалы

- Стоковые аудио - https://stock.adobe.com/ru/audio
- Стоковые видео - https://ru.freepik.com/videos
- Стоковые фото - https://ru.freepik.com/popular-photos

## Домашнее задание

Создай таблицу с рейтингом ваших любимых/нелюбимых блюд. В таблице должно быть минимум 5 рядов и 4 столбца:

1. Первый столбец - название блюда;
2. Второй столбец - ваша оценка от 1 до 10;
3. Третий столбец - ссылка на рецепт;
4. Четвертый столбец - фото блюда.

